# ─── Database connection ──────────────────────────────────────────────────────
# Matches docker-compose.yml defaults and backend/.env.example
DATABASE_URL=postgresql://rbac_user:rbac_password@localhost:5432/rbac_marketplace
